<div class="min-h-screen bg-gray-100 py-8 flex items-center justify-center">
  <div class="w-full max-w-md bg-white shadow-lg rounded-lg p-8 mx-auto">
    <h1 class="text-2xl font-bold mb-6 text-center text-blue-700">Sửa giao dịch chi tiêu</h1>
    <%= render 'form', transaction: @transaction, categories: @categories %>
    <div class="mt-4 text-center">
      <%= link_to 'Quay lại', transactions_path, class: 'text-blue-500 hover:underline font-semibold' %>
    </div>
  </div>
</div>
