<div class="container mx-auto p-4">
  <h1 class="text-2xl font-bold mb-4">Sửa danh mục chi tiêu</h1>
  <%= render 'form', category: @category %>
  <%= link_to 'Quay lại', categories_path, class: 'text-blue-500 underline' %>
</div>
