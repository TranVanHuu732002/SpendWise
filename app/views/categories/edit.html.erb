<div class="min-vh-100 bg-light py-4 d-flex align-items-center justify-content-center">
  <div class="container-sm" style="max-width: 540px;">
    <div class="card shadow">
      <div class="card-body p-4">
        <h1 class="h3 fw-bold mb-4 text-center text-primary">Sửa danh mục chi tiêu</h1>
        <%= render 'form', category: @category %>
        <div class="mt-3 text-center">
          <%= link_to 'Quay lại', categories_path, class: 'text-primary' %>
        </div>
      </div>
    </div>
  </div>
</div>
